name: Aurore School CI/CD
on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FLUTTER_VERSION: '3.19.5'  # Contains Dart 3.7.2 (matches your requirement)
    steps:
      - uses: actions/checkout@v4

      - name: Clean Install Flutter
        run: |
          # Remove conflicting installations
          sudo rm -rf /opt/hostedtoolcache/flutter
          
          # Install from verified archive
          wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_$FLUTTER_VERSION-stable.tar.xz
          tar xf flutter_linux_$FLUTTER_VERSION-stable.tar.xz -C ~
          echo "$HOME/flutter/bin" >> $GITHUB_PATH

      - name: Force Dart Version Check
        run: |
          echo "Updated PATH: $PATH"
          which flutter
          which dart
          flutter --version
          if ! dart --version | grep -q '3.7.2'; then
            echo "❌ Critical Error: Dart 3.7.2 not found!"
            exit 1
          fi

      - name: Build APK
        working-directory: ./aurore_frontend
        env:
          FLUTTER_SUPPRESS_ANALYTICS: true
        run: |
          flutter config --no-cli-animations
          flutter clean
          flutter pub get
          flutter build apk --release --verbose
