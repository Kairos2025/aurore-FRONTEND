name: Aurore School CI/CD
on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set Up Docker Build
        run: |
          # Create Dockerfile with exact environment
          echo 'FROM ubuntu:22.04
          RUN apt-get update && apt-get install -y \
              git \
              wget \
              unzip \
              xz-utils \
              libglu1-mesa \
              openjdk-17-jdk \
              && rm -rf /var/lib/apt/lists/*

          # Install Dart 3.8.0
          RUN wget https://storage.googleapis.com/dart-archive/channels/stable/release/3.8.0/sdk/dartsdk-linux-x64-release.zip -O dart.zip \
              && unzip dart.zip -d /usr/lib \
              && rm dart.zip \
              && ln -s /usr/lib/dart-sdk/bin/dart /usr/bin/dart

          # Install Flutter 3.22.2
          RUN git clone https://github.com/flutter/flutter.git -b 3.22.2 --depth 1 /usr/local/flutter
          ENV PATH="$PATH:/usr/local/flutter/bin"

          # Accept licenses
          RUN yes | flutter doctor --android-licenses
          WORKDIR /app' > Dockerfile

          # Build and verify
          docker build -t aurore-builder .

      - name: Run Build in Container
        run: |
          docker run --rm -v $PWD:/app aurore-builder bash -c "
            cd aurore_frontend &&
            flutter clean &&
            flutter pub upgrade --major-versions &&
            flutter build apk --release --verbose
          "

      - name: Extract Artifact
        run: |
          cp aurore_frontend/build/app/outputs/flutter-apk/app-release.apk .

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: app-release.apk
