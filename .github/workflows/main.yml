name: Build APK

on:
  push:
    branches:
      - update-gradle
  pull_request:
    branches:
      - update-gradle

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository with a specific path
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          path: aurore-FRONTEND

      # Set up Java (required for Android builds)
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0' # Latest stable version as of May 2025
          channel: 'stable'

      # Verify working directory
      - name: Verify directory
        run: |
          pwd
          ls -la .
          ls -la ./aurore-FRONTEND || { echo "Error: aurore-FRONTEND directory not found"; exit 1; }

      # Get Flutter dependencies
      - name: Install dependencies
        working-directory: ./aurore-FRONTEND
        run: flutter pub get

      # Build the APK
      - name: Build APK
        working-directory: ./aurore-FRONTEND
        run: |
          flutter build apk --release
          ls -la build/app/outputs/flutter-apk/ || { echo "Error: APK build failed or output not found"; exit 1; }

      # Upload the APK as an artifact
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: aurore-FRONTEND/aurore-FRONTEND/build/app/outputs/flutter-apk/app-release.apk
